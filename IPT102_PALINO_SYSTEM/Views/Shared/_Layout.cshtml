<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - My ASP.NET Application</title>
    <link href="~/Content/Style.css" rel="stylesheet" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" integrity="sha512-DTOQO9RWCH3ppGqcWaEA1BIZOC6xxalwEsw9c2QQeAIftl+Vegovlnee1c9QX4TctnWMn13TZye+giMm8e2LwA==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/regular.min.css" integrity="sha512-TzeemgHmrSO404wTLeBd76DmPp5TjWY/f2SyZC6/3LsutDYMVYfOx2uh894kr0j9UM6x39LFHKTeLn99iz378A==" crossorigin="anonymous" referrerpolicy="no-referrer" />
</head>
<body>
    @*<div class="topnav">
        @{
            var username = User.Identity.Name;
        }

        @Html.Label("Dashboard", "Welcome, " + username)
    </div>
    <div class="sidenav">
        <div class="profile-img">
             Add the path to the user's profile image 
            <img src="~/Content/Icon/12345.png" style="width: 35%; margin-left: 20px;" />
        </div>
        <div class="profile-info">
            <p>@User.Identity.Name</p>
            @if (User.IsInRole("Admin"))
            {
                <p>Admin</p>
            }
            else
            {
                <p>Patient</p>
            }
        </div>
        <a href="@Url.Action("AdminDashboard", "AdminDashboard", new { @class = "dropdown-btn" })">Admin Dashboard</a>
        <a class="dropdown-btn">Records of Patients</a>
        <div class="dropdown-container">
            <a href="@Url.Action("ListMedicalRecords", "ListMedicalRecords", new { @class = "btn-Records" })">Medical Records</a>
            <a href="@Url.Action("AdmissionsofRecords", "AdmissionsofRecords", new { @class = "btn-Records" })">Admissions of Records</a>
            <a href="@Url.Action("PrescriptionRecords", "PrescriptionRecords", new { @class = "" })">Prescription Records</a>
        </div>
        <a href="@Url.Action("Doctors", "Doctors", new { @class = "dropdown-btn" })">Doctors</a>
        <a href="@Url.Action("List", "List", new { @class = "dropdown-btn" })">List of Appointment</a>
        <a href="@Url.Action("ListofComplains", "ListofComplains", new { @class = "dropdown-btn" })">List of Complains</a>*@

    @*</div>*@
        @RenderBody()
    <script src="~/Content/Function.js"></script>
</body>
</html>